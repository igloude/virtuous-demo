# Plan 007: Document the exported theme API (`applyTheme`, `getStoredTheme`, `Theme`) in docs/tokens.md

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving on.
> If any STOP condition occurs, stop and report — do not improvise. When
> done, update this plan's status row in `plans/README.md`.
>
> **Drift check (run first)**: `git diff --stat 128dacd..HEAD -- docs/tokens.md src/tokens/theme.ts src/index.ts`
> If any of these changed since this plan was written, compare the
> "Current state" excerpts against live code; on mismatch, STOP.

## Status

- **Priority**: P2
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: contracts
- **Downstream effect**: gives the three non-component exports a doc unit, so an agent reading `docs/` (not the manifest) can discover `getStoredTheme` and the storage contract instead of re-implementing theme persistence — an `adoption.duplicate` generator today.
- **Planned at**: commit `128dacd`, 2026-08-19
- **Issue**: —

## Why this matters

`src/index.ts` exports three things that are not components: `type Theme`, `applyTheme`, `getStoredTheme`. Only `applyTheme` is mentioned in docs, and by *file path* rather than package import (`docs/tokens.md:41`). `getStoredTheme`, the `"vds-theme"` localStorage key, and the "`system` clears `data-theme`" behaviour are stated only in the manifest inventory — a generator working from the docs tree cannot find them. This plan adds a short "Theme API" section to `docs/tokens.md` with a signature table that matches the exported types exactly.

## Current state

- `src/tokens/theme.ts` (verbatim, the API of record):

  ```ts
  theme.ts:1   export type Theme = "light" | "dark" | "system";
  theme.ts:3   const STORAGE_KEY = "vds-theme";
  theme.ts:5   /** Apply a theme by setting (or clearing) `data-theme` on <html>. */
  theme.ts:6   export function applyTheme(theme: Theme): void {
  theme.ts:8-12  "system" → root.removeAttribute("data-theme"); else root.setAttribute("data-theme", theme)
  theme.ts:13-17 localStorage.setItem(STORAGE_KEY, theme) inside try/catch (storage failures ignored)
  theme.ts:20  /** Read the persisted theme preference, defaulting to "system". */
  theme.ts:21  export function getStoredTheme(): Theme {
  theme.ts:22-28 reads STORAGE_KEY; returns it only if "light" | "dark" | "system"; else "system"; try/catch
  ```

- `src/index.ts:1` — `export * from "./tokens";` → `src/tokens/index.ts:2` `export * from "./theme";` — so all three are public package exports.
- `docs/tokens.md:33-41` — the existing "Theming" section. Line 41 reads, verbatim:
  `Use \`applyTheme("light" | "dark" | "system")\` from \`src/tokens/theme.ts\` to switch at runtime; the choice is persisted to \`localStorage\`.`
- Doc conventions: component docs use a `## Props` table with columns `Prop | Type | Default | Description` (exemplar: `docs/components/link.md:14-17`); import lines use the package name, e.g. `docs/components/button.md:4` `import { Button } from "virtuous-demo";`.
- Manifest inventory already states (generated zone, `ds/MANIFEST.md`, inventory zone): "Also exported (token layer, `src/tokens/theme.ts`): `type Theme = "light" | "dark" | "system"`, `applyTheme(theme)`, `getStoredTheme()`." and notes-for-generators: "Theme: call `applyTheme("light" | "dark" | "system")`; do not set `data-theme` by hand." The doc must agree with these.
- Demo usage pattern (`src/demo/App.tsx:2,5,9`): `useState<Theme>(getStoredTheme)` + `useEffect(() => applyTheme(theme), [theme])`.

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Typecheck (repo gate) | `npm run typecheck` | exit 0 |
| Gap closed | `grep -n "getStoredTheme\|vds-theme" docs/tokens.md` | ≥ 2 lines |
| Scope guard | `git status --porcelain` | only `docs/tokens.md` and `plans/README.md` |

## Scope

**In scope**: `docs/tokens.md`, `plans/README.md` (status row).
**Out of scope**: `src/tokens/theme.ts` (source — if the doc would need a behaviour change to be true, STOP); `ds/MANIFEST.md` (regenerated by `/ds-doctor manifest`, not hand-edited here); `docs/components/*.md`; `README.md`.

## Git workflow

Branch `ds-docs/007-theme-api-docs`; one commit, message style matched to `git log`. Do NOT push or open a PR unless instructed.

## Steps

### Step 1: Replace the runtime sentence and add a Theme API table
In `docs/tokens.md`, replace line 41 (quoted in Current state) with the following block, verbatim:

```markdown
Switch themes at runtime with the exported theme API (imported from the package, not by file path):

```tsx
import { applyTheme, getStoredTheme, type Theme } from "virtuous-demo";

const initial: Theme = getStoredTheme(); // "system" until the user picks one
applyTheme(initial);
```

### Theme API

| Export | Type | Description |
|---|---|---|
| `Theme` | `"light" \| "dark" \| "system"` | The three accepted values. |
| `applyTheme` | `(theme: Theme) => void` | Sets `data-theme="light"`/`"dark"` on `<html>`; `"system"` removes the attribute so `prefers-color-scheme` applies. Persists the choice to `localStorage` under the key `"vds-theme"`; storage failures are ignored. |
| `getStoredTheme` | `() => Theme` | Reads `"vds-theme"` from `localStorage`; returns `"system"` when unset, unreadable, or not one of the three values. |

Do not set `data-theme` by hand — always go through `applyTheme` so the stored preference stays in sync.
```

(The fenced `tsx` block above is part of the inserted text; keep the fences. The outer fence in this plan is only to delimit the insertion.)
**Verify**: `grep -n "### Theme API\|getStoredTheme\|vds-theme" docs/tokens.md` → ≥ 3 lines; `grep -c "src/tokens/theme.ts" docs/tokens.md` → `0` after the edit (the file-path reference at old line 41 is gone; line 3's `src/tokens/tokens.css` reference is unrelated and stays).

### Step 2: Sanity-check the table against the types
Open `src/tokens/theme.ts` and confirm each row: union at line 1, `applyTheme` signature at line 6 and behaviour at 8-17, `getStoredTheme` at 21-29, key at line 3.
**Verify**: `grep -n 'STORAGE_KEY = "vds-theme"' src/tokens/theme.ts` → line 3.

## Test plan

None — docs only. No docs build exists (CLAUDE.md: "No tests, lint, docs build or Storybook exist").

## Done criteria

- [ ] `npm run typecheck` exits 0
- [ ] The gap is closed verbatim: `grep -n "getStoredTheme\|vds-theme" docs/tokens.md` → ≥ 2 lines
- [ ] `grep -c "src/tokens/theme.ts" docs/tokens.md` → `0`
- [ ] No source files modified (`git status` shows `docs/tokens.md` and `plans/README.md` only)
- [ ] plans/README.md status row updated

## STOP conditions

- `src/tokens/theme.ts` no longer matches the excerpts (different key, extra exports, changed union) — the table would be wrong.
- `docs/tokens.md` line 41 no longer contains the quoted sentence (section already rewritten).
- You find yourself wanting to change `theme.ts` to make the doc true.

## Maintenance notes

- Re-run `/ds-doctor manifest` after this lands: **yes** — the inventory zone already mentions the theme exports; regeneration keeps it consistent with the new doc.
- If the theme API ever gets its own doc page (`docs/theme.md`), move the table there and leave a one-line pointer in `docs/tokens.md`.
